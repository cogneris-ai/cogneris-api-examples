{
  "name": "cogneris-api-examples",
  "version": "1.0.0",
  "private": true,
  "description": "Runnable Cogneris API examples generated from OpenAPI",
  "scripts": {
    "generate": "node scripts/generate-postman.mjs",
    "generate:sdks": "node scripts/generate-sdks.mjs",
    "check:sdks": "node scripts/generate-sdks.mjs --check",
    "test:sdk:typescript": "node --test tests/sdk_typescript_smoke.mjs",
    "test:sdk:python": "PYTHONDONTWRITEBYTECODE=1 uv run --with 'httpx>=0.23,<0.29' --with 'attrs>=22.2' --with 'python-dateutil>=2.8,<3' python -m unittest tests.test_sdk_helpers.PythonInstalledSdkSmokeTests -v",
    "verify:sdks": "npm run check:sdks && npm run test:sdk:typescript && npm run test:sdk:python",
    "smoke:live": "node scripts/smoke-live.mjs",
    "test": "PYTHONDONTWRITEBYTECODE=1 uv run --with 'httpx>=0.23,<0.29' --with 'attrs>=22.2' --with 'python-dateutil>=2.8,<3' python -m unittest discover -s tests -p 'test_*.py'"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "0.99.0",
    "typescript": "5.9.3"
  }
}
