// <auto-generated>
/*
 * Cogneris Document AI API
 *
 * The Cogneris Document AI API turns unstructured documents — PDFs, images, scans — into structured JSON.  This document describes the endpoints that are actually deployed. It is written from the running service (`cogneris-api-be`) rather than from a design, so what is listed here is what you can call.  ## Authentication  Every endpoint takes an API key in the `Authorization` header as `Bearer <key>`. Keys begin with `xtkt_live_` (production) or `xtkt_test_` (sandbox); either prefix is accepted before the key is looked up for validation. The key selects the application environment. A recognized prefix alone does not authenticate a request: the key must also be valid and active. Keys are tenant-scoped and cannot cross tenant boundaries. Mint them in the dashboard under **Settings → API keys**.  ## Uploads  The synchronous document endpoints take `multipart/form-data`. There is no fetch-by-URL variant — the file travels in the request body.  - Accepted extensions: `.pdf`, `.png`, `.jpg`, `.jpeg`, `.tiff`, `.tif`,   `.bmp`, `.doc`, `.docx` - Maximum size: 10 MB per file, except `/Document/split`, which accepts 500 MB  The asynchronous job endpoints do not take a file. Upload it first to `POST /api/v1/artifacts` — same extensions, same 10 MB limit — and submit the `artifact://` reference it returns.  ## Artifact references  A reference names one stored object. `POST /api/v1/artifacts` returns one, a job submit consumes one as `inputReference`, and a finished job returns one as `outputReference`. `GET /api/v1/artifacts/content` turns either back into bytes.  - References are scoped to the tenant that created them. One belonging to another   tenant is refused as an invalid reference — the answer does not distinguish a   reference that exists elsewhere from one that never existed. - An uploaded reference is **reusable**: one upload can back several jobs. - It expires **7 days** after upload, matching how long a job is retained. The   exact instant is returned as `expiresAt`. Reading it afterwards answers `410`,   which is not `404` — the reference was real, it aged out, and re-requesting it   will never succeed. Upload again. - A job's `outputReference` lives as long as the job does.  ### Two shapes, one of them historical  Everything this API produces today is `artifact://<key>`. Jobs that completed before 2026-09-22 carry a bucket-qualified path instead — `<bucket>/<tenant>/document-jobs/<jobId>/result.json`, sometimes with an `@<version>` suffix. `GET /api/v1/artifacts/content` accepts both, so a reference stored from an older job still resolves for the rest of its life. Treat a reference as opaque: pass back exactly what you were given.  ## Templates  Extraction and classification are driven by the document types and templates configured for your tenant. The platform selects the template from the document itself, so the synchronous `/Document/_*` calls take no template or schema parameter. An asynchronous Extraction job may instead name one explicitly with `templateId` on `POST /api/v1/document-jobs`.  ## Scopes  Keys carry scopes. The Portal endpoints check them and answer `403` when the required one is absent; the document endpoints accept any valid key for the tenant without consulting the list. `*` is a real scope meaning full access, and it is what a key receives when it is created without an explicit scope list.  ## Rate limits  One shared policy covers every surface in this document, Portal included: **50 requests per API key per fixed 1-minute window**. It is a fixed window rather than a token bucket, so the allowance resets on the minute instead of refilling gradually. Requests over the limit receive `429`.  ## Response envelope  Successful responses from the **document and job** endpoints share one wrapper: `data` for the payload, `meta` for the status and any messages, and `hasErrors` for a fast failure check.  The **Portal** endpoints do not use that wrapper. They return the payload directly, and their errors are RFC 9457 problem documents sent as `application/problem+json`, with a stable `code`, a `correlationId` that is also returned in the `x-correlation-id` header, and a `retryable` flag. Branch on `code`; treat `type` as an opaque identifier.  ## Errors  An error arrives in one of two shapes, and the `Content-Type` says which.  - `application/problem+json` — an RFC 9457 problem document, the same one the   Portal uses. Every route answers this way when the failure happens before your   request is processed: a missing or invalid key (`401`), the rate limit (`429`),   a request refused as malformed or unsafe (`400`), or an unexpected failure   (`500`). - `application/json` — the service envelope with `hasErrors: true` and the   reasons in `meta.errors`. The document, job and artifact routes answer this way   when processing itself fails. Each operation lists the statuses where it does.  Every operation lists `400`, `401`, `403`, `404`, `409`, `429` and `500`, because the API answers each of them in the same shape wherever it occurs. A status on an operation with nothing to miss or conflict with — a `409` on a read, say — is part of that uniform model and not something the operation returns in practice.
 *
 * The version of the OpenAPI document: 2026-09-24
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */

#nullable enable

using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Linq;
using System.IO;
using System.Text;
using System.Text.RegularExpressions;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.ComponentModel.DataAnnotations;
using Cogneris.DocumentAI.Client;

namespace Cogneris.DocumentAI.Model
{
    /// <summary>
    /// ClassificationResult
    /// </summary>
    public partial class ClassificationResult : IValidatableObject
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="ClassificationResult" /> class.
        /// </summary>
        /// <param name="fileName">The uploaded file this entry classifies.</param>
        /// <param name="documentType">documentType</param>
        /// <param name="confidence">Certainty in &#x60;documentType&#x60;.</param>
        /// <param name="pages">pages</param>
        /// <param name="templateId">The template the document type resolves to, when one is configured.</param>
        /// <param name="templateMetadata">The matched template&#39;s schema. Null when &#x60;templateId&#x60; is, or the template has no schema yet.</param>
        [JsonConstructor]
        public ClassificationResult(Option<string?> fileName = default, Option<string?> documentType = default, Option<decimal?> confidence = default, Option<int?> pages = default, Option<Guid?> templateId = default, Option<string?> templateMetadata = default)
        {
            FileNameOption = fileName;
            DocumentTypeOption = documentType;
            ConfidenceOption = confidence;
            PagesOption = pages;
            TemplateIdOption = templateId;
            TemplateMetadataOption = templateMetadata;
            OnCreated();
        }

        partial void OnCreated();

        /// <summary>
        /// Used to track the state of FileName
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<string?> FileNameOption { get; private set; }

        /// <summary>
        /// The uploaded file this entry classifies.
        /// </summary>
        /// <value>The uploaded file this entry classifies.</value>
        [JsonPropertyName("fileName")]
        public string? FileName { get { return this.FileNameOption.Value; } set { this.FileNameOption = new(value); } }

        /// <summary>
        /// Used to track the state of DocumentType
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<string?> DocumentTypeOption { get; private set; }

        /// <summary>
        /// Gets or Sets DocumentType
        /// </summary>
        [JsonPropertyName("documentType")]
        public string? DocumentType { get { return this.DocumentTypeOption.Value; } set { this.DocumentTypeOption = new(value); } }

        /// <summary>
        /// Used to track the state of Confidence
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<decimal?> ConfidenceOption { get; private set; }

        /// <summary>
        /// Certainty in &#x60;documentType&#x60;.
        /// </summary>
        /// <value>Certainty in &#x60;documentType&#x60;.</value>
        [JsonPropertyName("confidence")]
        public decimal? Confidence { get { return this.ConfidenceOption.Value; } set { this.ConfidenceOption = new(value); } }

        /// <summary>
        /// Used to track the state of Pages
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<int?> PagesOption { get; private set; }

        /// <summary>
        /// Gets or Sets Pages
        /// </summary>
        [JsonPropertyName("pages")]
        public int? Pages { get { return this.PagesOption.Value; } set { this.PagesOption = new(value); } }

        /// <summary>
        /// Used to track the state of TemplateId
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<Guid?> TemplateIdOption { get; private set; }

        /// <summary>
        /// The template the document type resolves to, when one is configured.
        /// </summary>
        /// <value>The template the document type resolves to, when one is configured.</value>
        [JsonPropertyName("templateId")]
        public Guid? TemplateId { get { return this.TemplateIdOption.Value; } set { this.TemplateIdOption = new(value); } }

        /// <summary>
        /// Used to track the state of TemplateMetadata
        /// </summary>
        [JsonIgnore]
        [global::System.ComponentModel.EditorBrowsable(global::System.ComponentModel.EditorBrowsableState.Never)]
        public Option<string?> TemplateMetadataOption { get; private set; }

        /// <summary>
        /// The matched template&#39;s schema. Null when &#x60;templateId&#x60; is, or the template has no schema yet.
        /// </summary>
        /// <value>The matched template&#39;s schema. Null when &#x60;templateId&#x60; is, or the template has no schema yet.</value>
        [JsonPropertyName("templateMetadata")]
        public string? TemplateMetadata { get { return this.TemplateMetadataOption.Value; } set { this.TemplateMetadataOption = new(value); } }

        /// <summary>
        /// Returns the string presentation of the object
        /// </summary>
        /// <returns>String presentation of the object</returns>
        public override string ToString()
        {
            StringBuilder sb = new StringBuilder();
            sb.Append("class ClassificationResult {\n");
            sb.Append("  FileName: ").Append(FileName).Append("\n");
            sb.Append("  DocumentType: ").Append(DocumentType).Append("\n");
            sb.Append("  Confidence: ").Append(Confidence).Append("\n");
            sb.Append("  Pages: ").Append(Pages).Append("\n");
            sb.Append("  TemplateId: ").Append(TemplateId).Append("\n");
            sb.Append("  TemplateMetadata: ").Append(TemplateMetadata).Append("\n");
            sb.Append("}\n");
            return sb.ToString();
        }

        /// <summary>
        /// To validate all properties of the instance
        /// </summary>
        /// <param name="validationContext">Validation context</param>
        /// <returns>Validation Result</returns>
        IEnumerable<ValidationResult> IValidatableObject.Validate(ValidationContext validationContext)
        {
            yield break;
        }
    }

    /// <summary>
    /// A Json converter for type <see cref="ClassificationResult" />
    /// </summary>
    public partial class ClassificationResultJsonConverter : JsonConverter<ClassificationResult>
    {
        partial void OnCreated();

        /// <summary>
        /// Initializes a new instance of the <see cref="ClassificationResultJsonConverter" /> class.
        /// </summary>
        public ClassificationResultJsonConverter()
        {
            OnCreated();
        }

        /// <summary>
        /// Deserializes json to <see cref="ClassificationResult" />
        /// </summary>
        /// <param name="utf8JsonReader"></param>
        /// <param name="typeToConvert"></param>
        /// <param name="jsonSerializerOptions"></param>
        /// <returns></returns>
        /// <exception cref="JsonException"></exception>
        public override ClassificationResult Read(ref Utf8JsonReader utf8JsonReader, Type typeToConvert, JsonSerializerOptions jsonSerializerOptions)
        {
            int currentDepth = utf8JsonReader.CurrentDepth;

            if (utf8JsonReader.TokenType != JsonTokenType.StartObject && utf8JsonReader.TokenType != JsonTokenType.StartArray)
                throw new JsonException();

            JsonTokenType startingTokenType = utf8JsonReader.TokenType;

            Option<string?> fileName = default;
            Option<string?> documentType = default;
            Option<decimal?> confidence = default;
            Option<int?> pages = default;
            Option<Guid?> templateId = default;
            Option<string?> templateMetadata = default;

            while (utf8JsonReader.Read())
            {
                if (startingTokenType == JsonTokenType.StartObject && utf8JsonReader.TokenType == JsonTokenType.EndObject && currentDepth == utf8JsonReader.CurrentDepth)
                    break;

                if (startingTokenType == JsonTokenType.StartArray && utf8JsonReader.TokenType == JsonTokenType.EndArray && currentDepth == utf8JsonReader.CurrentDepth)
                    break;

                if (utf8JsonReader.TokenType == JsonTokenType.PropertyName && currentDepth == utf8JsonReader.CurrentDepth - 1)
                {
                    string? localVarJsonPropertyName = utf8JsonReader.GetString();
                    utf8JsonReader.Read();

                    switch (localVarJsonPropertyName)
                    {
                        case "fileName":
                            fileName = new Option<string?>(utf8JsonReader.GetString()!);
                            break;
                        case "documentType":
                            documentType = new Option<string?>(utf8JsonReader.GetString()!);
                            break;
                        case "confidence":
                            confidence = new Option<decimal?>(utf8JsonReader.TokenType == JsonTokenType.Null ? (decimal?)null : utf8JsonReader.GetDecimal());
                            break;
                        case "pages":
                            pages = new Option<int?>(utf8JsonReader.TokenType == JsonTokenType.Null ? (int?)null : utf8JsonReader.GetInt32());
                            break;
                        case "templateId":
                            templateId = new Option<Guid?>(utf8JsonReader.TokenType == JsonTokenType.Null ? (Guid?)null : utf8JsonReader.GetGuid());
                            break;
                        case "templateMetadata":
                            templateMetadata = new Option<string?>(utf8JsonReader.GetString());
                            break;
                        default:
                            break;
                    }
                }
            }

            if (fileName.IsSet && fileName.Value == null)
                throw new ArgumentNullException(nameof(fileName), "Property is not nullable for class ClassificationResult.");

            if (documentType.IsSet && documentType.Value == null)
                throw new ArgumentNullException(nameof(documentType), "Property is not nullable for class ClassificationResult.");

            if (confidence.IsSet && confidence.Value == null)
                throw new ArgumentNullException(nameof(confidence), "Property is not nullable for class ClassificationResult.");

            return new ClassificationResult(fileName, documentType, confidence, pages, templateId, templateMetadata);
        }

        /// <summary>
        /// Serializes a <see cref="ClassificationResult" />
        /// </summary>
        /// <param name="writer"></param>
        /// <param name="classificationResult"></param>
        /// <param name="jsonSerializerOptions"></param>
        /// <exception cref="NotImplementedException"></exception>
        public override void Write(Utf8JsonWriter writer, ClassificationResult classificationResult, JsonSerializerOptions jsonSerializerOptions)
        {
            writer.WriteStartObject();

            WriteProperties(writer, classificationResult, jsonSerializerOptions);
            writer.WriteEndObject();
        }

        /// <summary>
        /// Serializes the properties of <see cref="ClassificationResult" />
        /// </summary>
        /// <param name="writer"></param>
        /// <param name="classificationResult"></param>
        /// <param name="jsonSerializerOptions"></param>
        /// <exception cref="NotImplementedException"></exception>
        public void WriteProperties(Utf8JsonWriter writer, ClassificationResult classificationResult, JsonSerializerOptions jsonSerializerOptions)
        {
            if (classificationResult.FileNameOption.IsSet && classificationResult.FileName == null)
                throw new ArgumentNullException(nameof(classificationResult.FileName), "Property is required for class ClassificationResult.");

            if (classificationResult.DocumentTypeOption.IsSet && classificationResult.DocumentType == null)
                throw new ArgumentNullException(nameof(classificationResult.DocumentType), "Property is required for class ClassificationResult.");

            if (classificationResult.FileNameOption.IsSet)
                writer.WriteString("fileName", classificationResult.FileName);

            if (classificationResult.DocumentTypeOption.IsSet)
                writer.WriteString("documentType", classificationResult.DocumentType);

            if (classificationResult.ConfidenceOption.IsSet)
                writer.WriteNumber("confidence", classificationResult.ConfidenceOption.Value!.Value);

            if (classificationResult.PagesOption.IsSet)
                if (classificationResult.PagesOption.Value != null)
                    writer.WriteNumber("pages", classificationResult.PagesOption.Value!.Value);
                else
                    writer.WriteNull("pages");

            if (classificationResult.TemplateIdOption.IsSet)
                if (classificationResult.TemplateIdOption.Value != null)
                    writer.WriteString("templateId", classificationResult.TemplateIdOption.Value!.Value);
                else
                    writer.WriteNull("templateId");

            if (classificationResult.TemplateMetadataOption.IsSet)
                if (classificationResult.TemplateMetadataOption.Value != null)
                    writer.WriteString("templateMetadata", classificationResult.TemplateMetadata);
                else
                    writer.WriteNull("templateMetadata");
        }
    }
}
