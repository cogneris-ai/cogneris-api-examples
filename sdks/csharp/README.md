# Cogneris Document AI SDK for C#

Package: `Cogneris.DocumentAI`, version `0.1.0`. Requires .NET 8.

The typed client is generated from the repository's public OpenAPI contract.
Regenerate all SDKs from the repository root with `npm run generate:sdks`;
verify committed output with `npm run check:sdks`.

Build the package using the explicit project path:

```sh
dotnet build src/Cogneris.DocumentAI/Cogneris.DocumentAI.csproj
dotnet pack src/Cogneris.DocumentAI/Cogneris.DocumentAI.csproj
```

Licensed under Apache-2.0. See `LICENSE` and `NOTICE`, included in the NuGet package.
